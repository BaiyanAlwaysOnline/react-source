import React from "../lib/react";

function FunctionComponent() {
  return (
    <div id="counter">
      <span>{0}</span>
      <button>加一</button>
    </div>
  );
}

export default FunctionComponent;
